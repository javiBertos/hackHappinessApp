'use strict';

describe('hackHappinessApp.explore module', function() {

  beforeEach(module('hackHappinessApp.explore'));

  describe('explore controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var exploreCtrl = $controller('ExploreCtrl');
      expect(exploreCtrl).toBeDefined();
    }));

  });
});